# AWS Workers

## Summary
These are AWS Lambda functions that handle the coral segmentation. View  the individual functions for details.

- `restoration`: Implements a series of image manipulations to restore an underwater image 
- `rotate-crop`: Rotates and crops images to a square as defined by a metal frame 
- `segmentation`: Detects corals on the images